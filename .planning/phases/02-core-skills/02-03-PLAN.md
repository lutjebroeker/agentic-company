---
phase: 02-core-skills
plan: "03"
type: execute
wave: 2
depends_on: ["02-01", "02-02"]
files_modified: []
autonomous: false
requirements: [CORE-03, CORE-04, CORE-05]

must_haves:
  truths:
    - "Maandag 07:00 → content briefing bericht arriveert in Telegram"
    - "Dagelijks 09:00 → DM-suggesties thread verschijnt in Slack #agent-sales"
    - "Zondag 18:00 → /learn synthesize output arriveert in Telegram + learning/improvements.md is bijgewerkt"
    - "Alle 3 workflows draaien zonder handmatige interventie"
  artifacts:
    - path: "n8n workflow: 02-03a-content-briefing (actief)"
      provides: "Schedule maandag 07:00 → SSH cat learning/ → SSH claude /learn → Telegram"
    - path: "n8n workflow: 02-03b-dm-suggestions (actief)"
      provides: "Schedule dagelijks 09:00 → SSH cat pipeline.md → SSH claude /sales → Slack #agent-sales"
    - path: "n8n workflow: 02-03c-learn-synthesize (actief)"
      provides: "Schedule zondag 18:00 → SSH cat learning/ → SSH claude /learn → Telegram + SSH schrijf improvements.md"
  key_links:
    - from: "Schedule Trigger (maandag 07:00)"
      to: "SSH cat wins.md + fails.md + improvements.md"
      via: "cron: 0 7 * * 1, timezone: Europe/Amsterdam"
    - from: "SSH cat pipeline.md"
      to: "SSH claude /sales prompt"
      via: "bestandsinhoud als prompt-context"
    - from: "SSH claude /learn output"
      to: "SSH tee improvements.md"
      via: "stdout → append naar bestand (zondag workflow)"
---

<objective>
Bouw de 3 proactieve scheduled workflows die automatisch op vaste tijdstippen output pushen naar Telegram en Slack, zonder dat Jelle iets hoeft te doen.

Purpose: Vervangt handmatige wekelijkse reviews en DM-planning — het systeem brengt het naar Jelle, niet andersom.
Output: 3 actieve n8n workflows: content briefing (maandag), DM-suggesties (dagelijks), learn synthesize (zondag).
</objective>

<execution_context>
@/root/.claude/get-shit-done/workflows/execute-plan.md
@/root/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-core-skills/02-CONTEXT.md
@.planning/phases/02-core-skills/02-RESEARCH.md
@.planning/phases/02-core-skills/02-01-SUMMARY.md
@.planning/phases/02-core-skills/02-02-SUMMARY.md

<interfaces>
<!-- SSH bridge is gevalideerd in 02-01 — gebruik zelfde credentials en pad -->

Bestanden op Proxmox host die gelezen worden:
- /root/projects/agentic-company/learning/wins.md
- /root/projects/agentic-company/learning/fails.md
- /root/projects/agentic-company/learning/improvements.md
- /root/projects/agentic-company/memory/pipeline.md

Schedule Trigger cron expressies (timezone: Europe/Amsterdam):
```javascript
// Maandag 07:00
{ "expression": "0 7 * * 1" }

// Dagelijks 09:00
{ "expression": "0 9 * * *" }

// Zondag 18:00
{ "expression": "0 18 * * 0" }
```

SSH commando om meerdere bestanden samen te lezen:
```bash
echo "=== WINS ===" && cat /root/projects/agentic-company/learning/wins.md
echo "=== FAILS ===" && cat /root/projects/agentic-company/learning/fails.md
echo "=== IMPROVEMENTS ===" && cat /root/projects/agentic-company/learning/improvements.md
```

SSH commando voor zondag improvements.md update (append nieuwe synthesize-output):
```bash
echo "\n## $(date +%Y-%m-%d) — Weekly Synthesize\n${SYNTHESIZE_OUTPUT}" >> /root/projects/agentic-company/learning/improvements.md
```

DM-suggesties prompt voor /sales skill:
```
Bekijk de pipeline hieronder. Geef voor vandaag 3-5 concrete DM-suggesties op basis van:
1. Leads waarvan de laatste contactdatum meer dan 5 dagen geleden is
2. Leads die naar de volgende fase kunnen bewegen
3. Eventuele aantekeningen over LinkedIn engagement

Wees concreet: "Stuur [Naam] een DM met [aanpak]" — niet algemeen.

Pipeline:
{pipeline_content}
```

Content briefing prompt voor /learn skill:
```
Bekijk de wins, fails en improvements van de afgelopen week. Genereer een gestructureerde content briefing:

1. Wat werkte goed? (1-2 bullets die verhaalwaardig zijn)
2. Wat mislukte of was inefficiënt? (eerlijk, niet beschuldigend)
3. Welk verhaal zit hierin voor LinkedIn? (1 concrete angle)
4. Welke inzichten zijn relevant voor volgende week?

Houd het bondig — dit is input voor Ziplined, niet de post zelf.

Leerlog:
{learning_content}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bouw 02-03a-content-briefing (maandag 07:00)</name>
  <files>n8n workflow (via n8n MCP — geen lokaal bestand)</files>
  <action>
    Gebruik n8n MCP om workflow "02-03a-content-briefing" aan te maken:

    **Node 1 — Schedule Trigger**
    - Type: n8n-nodes-base.scheduleTrigger
    - Cron: `0 7 * * 1` (maandag 07:00)
    - Timezone: Europe/Amsterdam

    **Node 2 — SSH: Lees learning bestanden**
    - Type: n8n-nodes-base.ssh
    - Credentials: SSH credentials (van 02-01)
    - Command:
      ```bash
      echo "=== WINS ===" && cat /root/projects/agentic-company/learning/wins.md && echo "" && echo "=== FAILS ===" && cat /root/projects/agentic-company/learning/fails.md && echo "" && echo "=== IMPROVEMENTS ===" && cat /root/projects/agentic-company/learning/improvements.md
      ```

    **Node 3 — Code: Bouw briefing prompt**
    - Type: n8n-nodes-base.code
    - Logica:
      ```javascript
      const learningContent = $json.stdout || '(geen data gevonden)';
      const learningEscaped = learningContent.replace(/`/g, "'").replace(/"/g, '\\"');

      const promptIntro = `Bekijk de wins, fails en improvements van de afgelopen week. Genereer een gestructureerde content briefing:

1. Wat werkte goed? (1-2 bullets die verhaalwaardig zijn)
2. Wat mislukte of was inefficiënt? (eerlijk, niet beschuldigend)
3. Welk verhaal zit hierin voor LinkedIn? (1 concrete angle)
4. Welke inzichten zijn relevant voor volgende week?

Houd het bondig — dit is input voor Ziplined, niet de post zelf.

Leerlog:
${learningEscaped}`;

      const skillPath = '/root/.claude/commands/learn.md';
      const command = `SKILL_PROMPT=$(cat ${skillPath})
/usr/local/bin/claude --no-permissions-prompt -p "$SKILL_PROMPT

${promptIntro}"`;

      return [{ json: { sshCommand: command } }];
      ```

    **Node 4 — SSH: Roep Claude /learn aan**
    - Type: n8n-nodes-base.ssh
    - Credentials: SSH credentials
    - Command: `{{$json.sshCommand}}`

    **Node 5 — Code: Formatteer voor Telegram**
    - Type: n8n-nodes-base.code
    - Logica:
      ```javascript
      const response = $json.stdout || 'Content briefing kon niet gegenereerd worden.';
      const header = '📋 *Content Briefing — week van ' + new Date().toLocaleDateString('nl-NL', {day: 'numeric', month: 'long'}) + '*\n\n';
      const fullMessage = (header + response).substring(0, 4096);
      return [{ json: { message: fullMessage } }];
      ```

    **Node 6 — Telegram: Stuur briefing**
    - Type: n8n-nodes-base.telegram
    - Operation: sendMessage
    - Chat ID: TELEGRAM_CHAT_ID (gebruik n8n credential of hardcode de chat ID uit 02-01 setup)
    - Text: `{{$json.message}}`
    - Parse Mode: Markdown

    Activeer workflow via n8n MCP.
  </action>
  <verify>
    Via n8n MCP: workflow "02-03a-content-briefing" heeft status "active".
    Voer een handmatige test-uitvoering uit via n8n (Execute Workflow knop) — controleer dat een Telegram bericht arriveert met content briefing tekst.
  </verify>
  <done>
    Workflow actief. Handmatige test toont Telegram bericht met gestructureerde content briefing gebaseerd op learning/wins.md + fails.md + improvements.md.
  </done>
</task>

<task type="auto">
  <name>Task 2: Bouw 02-03b-dm-suggestions + 02-03c-learn-synthesize</name>
  <files>n8n workflows (via n8n MCP — geen lokale bestanden)</files>
  <action>
    Bouw beide resterende proactieve workflows.

    ---

    **WORKFLOW 02-03b-dm-suggestions (dagelijks 09:00 → Slack #agent-sales)**

    **Node 1 — Schedule Trigger**
    - Cron: `0 9 * * *` (dagelijks 09:00)
    - Timezone: Europe/Amsterdam

    **Node 2 — SSH: Lees pipeline.md**
    - Command: `cat /root/projects/agentic-company/memory/pipeline.md`

    **Node 3 — Code: Bouw DM-suggesties prompt**
    - Logica:
      ```javascript
      const pipelineContent = $json.stdout || '(pipeline.md is leeg of niet gevonden)';
      const pipelineEscaped = pipelineContent.replace(/`/g, "'").replace(/"/g, '\\"');

      const prompt = `Bekijk de pipeline hieronder. Geef voor vandaag 3-5 concrete DM-suggesties op basis van:
1. Leads waarvan de laatste contactdatum meer dan 5 dagen geleden is
2. Leads die naar de volgende fase kunnen bewegen
3. Eventuele aantekeningen over LinkedIn engagement

Wees concreet: "Stuur [Naam] een DM met [aanpak]" - niet algemeen.
Vandaag is ${new Date().toLocaleDateString('nl-NL', {weekday: 'long', day: 'numeric', month: 'long'})}.

Pipeline:
${pipelineEscaped}`;

      const skillPath = '/root/.claude/commands/sales.md';
      const command = `SKILL_PROMPT=$(cat ${skillPath})
/usr/local/bin/claude --no-permissions-prompt -p "$SKILL_PROMPT

${prompt}"`;

      return [{ json: { sshCommand: command } }];
      ```

    **Node 4 — SSH: Roep Claude /sales aan**
    - Command: `{{$json.sshCommand}}`

    **Node 5 — Code: Formatteer voor Slack**
    - Logica:
      ```javascript
      const response = $json.stdout || 'DM-suggesties konden niet gegenereerd worden.';
      const date = new Date().toLocaleDateString('nl-NL', {weekday: 'long', day: 'numeric', month: 'long'});
      const header = `*DM-suggesties ${date}*\n\n`;
      const fullMessage = (header + response).substring(0, 4000);
      return [{ json: { message: fullMessage } }];
      ```

    **Node 6 — Slack: Post in #agent-sales**
    - Type: n8n-nodes-base.slack
    - Resource: message, Operation: post
    - Channel: PLACEHOLDER_SALES_CHANNEL_ID (vervang met werkelijke ID uit 02-02)
    - Text: `{{$json.message}}`
    - (geen thread_ts — dit start een nieuwe thread elke dag)

    Activeer workflow.

    ---

    **WORKFLOW 02-03c-learn-synthesize (zondag 18:00 → Telegram + improvements.md update)**

    **Node 1 — Schedule Trigger**
    - Cron: `0 18 * * 0` (zondag 18:00)
    - Timezone: Europe/Amsterdam

    **Node 2 — SSH: Lees learning bestanden**
    - Zelfde command als 02-03a Node 2 (alle drie bestanden)

    **Node 3 — Code: Bouw synthesize prompt**
    - Logica:
      ```javascript
      const learningContent = $json.stdout || '(geen data)';
      const learningEscaped = learningContent.replace(/`/g, "'").replace(/"/g, '\\"');
      const weekDate = new Date().toLocaleDateString('nl-NL', {day: 'numeric', month: 'long', year: 'numeric'});

      const prompt = `Synthesize de leerlog van deze week (week eindigend ${weekDate}):

1. Wat werkte goed? (max 3 bullets)
2. Wat werkte niet? (max 3 bullets)
3. Één concrete aanpassing voor volgende week (specifiek en uitvoerbaar)

Houd het kort en eerlijk. Dit gaat rechtstreeks naar improvements.md.

Leerlog:
${learningEscaped}`;

      const skillPath = '/root/.claude/commands/learn.md';
      const command = `SKILL_PROMPT=$(cat ${skillPath})
/usr/local/bin/claude --no-permissions-prompt -p "$SKILL_PROMPT

${prompt}"`;

      return [{ json: { sshCommand: command, weekDate } }];
      ```

    **Node 4 — SSH: Roep Claude /learn aan**
    - Command: `{{$json.sshCommand}}`

    **Node 5 — Code: Sla output op als variabele**
    - Logica:
      ```javascript
      const synthesizeOutput = $json.stdout || '(geen output)';
      const weekDate = $('Code: Bouw synthesize prompt').first().json.weekDate;
      const outputEscaped = synthesizeOutput.replace(/'/g, "'\\''"); // shell escaping
      const appendCommand = `printf '\\n## ${weekDate} — Weekly Synthesize\\n${outputEscaped}\\n' >> /root/projects/agentic-company/learning/improvements.md`;
      const telegramMsg = ('*Wekelijkse Synthesize*\n\n' + synthesizeOutput).substring(0, 4096);
      return [{ json: { appendCommand, telegramMsg } }];
      ```

    **Node 6 — SSH: Update improvements.md**
    - Command: `{{$json.appendCommand}}`

    **Node 7 — Telegram: Stuur synthesize output**
    - Type: n8n-nodes-base.telegram
    - Operation: sendMessage
    - Chat ID: TELEGRAM_CHAT_ID
    - Text: `{{$json.telegramMsg}}`
    - Parse Mode: Markdown

    Activeer workflow.
  </action>
  <verify>
    Via n8n MCP: beide workflows "02-03b-dm-suggestions" en "02-03c-learn-synthesize" hebben status "active".
    Voer handmatige test-uitvoering uit voor beide:
    - 02-03b: Slack bericht verschijnt in #agent-sales
    - 02-03c: Telegram bericht arriveert + verbeterde improvements.md (SSH cat om te controleren)
  </verify>
  <done>
    Alle 3 proactieve workflows actief. Handmatige test-uitvoering succesvol voor elk. improvements.md bevat nieuw synthesize-blok na 02-03c test.
  </done>
</task>

<task type="checkpoint:human-verify">
  <name>Task 3: Verifieer alle proactieve workflows</name>
  <what-built>3 proactieve scheduled workflows actief: 02-03a (content briefing), 02-03b (DM-suggesties), 02-03c (learn synthesize).</what-built>
  <how-to-verify>
    1. **n8n overzicht:** Ga naar n8n → Workflows → controleer dat alle 5 workflows (02-01 t/m 02-03c) de status "active" tonen

    2. **Test 02-03b handmatig:** Open n8n → workflow 02-03b-dm-suggestions → klik "Execute Workflow" → controleer of Slack #agent-sales een nieuw bericht toont met DM-suggesties

    3. **Test 02-03a handmatig:** Open n8n → workflow 02-03a-content-briefing → klik "Execute Workflow" → controleer of Telegram een content briefing bericht toont

    4. **Test 02-03c handmatig:** Open n8n → workflow 02-03c-learn-synthesize → klik "Execute Workflow" → controleer (a) Telegram bericht aankomt, (b) improvements.md een nieuw blok heeft via SSH: `tail -20 /root/projects/agentic-company/learning/improvements.md`

    5. **Phase success check:**
       - [ ] Telegram bericht sturen → antwoord binnen 10 sec (02-01)
       - [ ] Slack #agent-sales bericht → thread reply (02-02)
       - [ ] Maandag 07:00 briefing in Telegram (02-03a — verified via handmatige test)
       - [ ] Dagelijks 09:00 DM-suggesties in Slack (02-03b — verified via handmatige test)
       - [ ] Zondag 18:00 synthesize in Telegram + improvements.md update (02-03c — verified)
  </how-to-verify>
  <resume-signal>Typ "approved" als alle 5 workflows actief zijn en handmatige tests slagen, of beschrijf welke workflow problemen heeft</resume-signal>
  <files>n8n workflows: 02-03a, 02-03b, 02-03c</files>
  <action>Human verificatie — zie how-to-verify voor stappen.</action>
  <verify>Human bevestigt met resume-signal.</verify>
  <done>Alle 3 proactieve workflows actief en handmatig getest. Phase 2 success criteria bereikt.</done>
</task>

</tasks>

<verification>
- Alle 3 workflows actief in n8n
- 02-03a: SSH leest alle 3 learning-bestanden, Claude genereert briefing, Telegram ontvangt bericht
- 02-03b: SSH leest pipeline.md, Claude genereert DM-suggesties, Slack #agent-sales ontvangt bericht
- 02-03c: SSH leest learning-bestanden, Claude synthesizeert, Telegram ontvangt bericht, improvements.md bijgewerkt
- Cron expressies correct ingesteld met Europe/Amsterdam timezone
</verification>

<success_criteria>
Alle 5 Phase 2 workflows draaien autonoom. De 3 proactieve workflows zijn getest via handmatige uitvoering. Phase 2 success criteria uit ROADMAP.md zijn bereikt: maandag briefing, dagelijkse DM-suggesties, zondag synthesize — allemaal zonder handmatige actie van Jelle.
</success_criteria>

<output>
Na completion, maak `.planning/phases/02-core-skills/02-03-SUMMARY.md` aan.
</output>
